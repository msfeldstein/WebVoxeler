const Voxel = require('./Voxel')

class VoxelGrid {
  constructor() {
    this.gridSize = gridSize
    for (var x = - 10; x < 10; x += gridSize) {
      for (var y = 0; y < 10; y+= gridSize) {
        for (var z = -10; z < 10; z += gridSize) {

        }
      }
    }
  }
}

module.exports = VoxelGrid
